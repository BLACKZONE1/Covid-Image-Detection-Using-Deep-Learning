Flask==2.3.3
numpy==1.26.4
opencv-python==4.9.0.80
tensorflow==2.15.0
✅ Note: Versions are based on compatibility with your app.py and typical setups. Adjust if you trained models with different TensorFlow versions.

pip install -r requirements.txt
